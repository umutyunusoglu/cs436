{
  "name": "price-tracker-infra",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for Gold & Silver Price Tracker",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy:all": "cdk deploy --all --require-approval never"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.130.0",
    "constructs": "^10.3.0",
    "@aws-cdk/aws-lambda-python-alpha": "^2.130.0-alpha.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "aws-cdk": "^2.130.0"
  }
}
